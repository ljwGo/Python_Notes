# ###
'''
如果不在家目录中,权限不够就加sudo
mv /路径/文件夹或文件 /路径/文件夹或文件(新名称) 功能:实现剪切或是改名
可使用相对路径,也可以是绝对路径

cp 路径 路径(从哪里 到 哪里去)(新名称) 复制并且改名
cp 默认只能复制文件
cp -r 增加参数-r可以复制文件夹(目录)

cp -a 连同权限,属性,内容和修改时间一并复制
cp -a ceshi100 ceshi600

# 把文件ceshi100文件夹赋予最高权限
chmod 777 ceshi100

# rm -rf 指定路径 删除文件或文件夹

#nano 文件名 内置自带的编辑器 编辑文件用的

#cat 1.py 直接打印文件内容(查看,一次性)

#more 分屏显示(按空格下一页

#head 加上-2 是查看前2行

#tail 加上-3 是查看后3行






'''