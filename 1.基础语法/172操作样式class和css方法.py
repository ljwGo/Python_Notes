'''
修改样式
$().css("样式名称","样式值").css("样式名称","样式值")
$().css({"样式名称1":"样式值1","样式名称2":"样式值2",......})
$().css("样式名称","function(){
    code...
    return ...
}")

jquery获取样式
$().css("样式名称");

$().addClass("") 给指定的所有元素添加class类
$().removeClass() 给指定的所有元素移除class类
$().toggleClass() 给获取到的所有元素进行判断,如果有指定class的则删除,如果没有指定class则添加

'''