day01：文件高级及函数基本使用
    1、文件类型
        x模式（控制文件操作的模式）
            x，只写模式【不可读；不存在则创建，存在则报错】

        b模式补充（控制文件读写内容的模式）
            bytes类型转换
                x = 10 # int(10)
                '你'.encode('gbk') # bytes()
    2、文件操作的其他方法

    3、文件的高级操作：控制文件指针的移动

    4、文件修改的两种方式

    5、函数的基本使用
        先定义
            三种定义方式
        后调用
            三种调用方式
        返回值
            三种返回值的形式

day02：函数参数

day03：名称空间与作用域、函数对象、闭包函数（尤其注意听）

day04：无参装饰器、有参装饰器

day05：可迭代对象：迭代器、生成器（自定义的迭代器）

装饰器基本原理
def zhuangshi(func):
    def new_func():
        print('新的code方法')
        func()
    return new_func
